{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "type": "stat",
      "title": "线索总数",
      "id": 1,
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 0 },
      "targets": [
        { "expr": "sum(admissions_leads_total)", "refId": "A" }
      ]
    },
    {
      "type": "stat",
      "title": "成交总数",
      "id": 2,
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 0 },
      "targets": [
        { "expr": "sum(admissions_deals_total)", "refId": "A" }
      ]
    },
    {
      "type": "stat",
      "title": "累计分成（元）",
      "id": 3,
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 0 },
      "targets": [
        { "expr": "sum(admissions_commission_fen) / 100", "refId": "A" }
      ]
    },
    {
      "type": "stat",
      "title": "已入账定金（元）",
      "id": 4,
      "gridPos": { "h": 4, "w": 6, "x": 18, "y": 0 },
      "targets": [
        { "expr": "sum(admissions_deposits_amount_fen{status=\"paid\"}) / 100", "refId": "A" }
      ]
    },
    {
      "type": "timeseries",
      "title": "作业队列 · 按状态",
      "id": 10,
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 4 },
      "targets": [
        { "expr": "sum(admissions_jobs_total) by (status)", "legendFormat": "{{status}}" }
      ]
    },
    {
      "type": "timeseries",
      "title": "过去 24h 作业成功数 · 按名称",
      "id": 11,
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 4 },
      "targets": [
        { "expr": "admissions_jobs_succeeded_last_day", "legendFormat": "{{name}}" }
      ]
    },
    {
      "type": "bargauge",
      "title": "线索按租户分布",
      "id": 20,
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 12 },
      "targets": [
        { "expr": "sum(admissions_leads_total) by (tenant)", "legendFormat": "{{tenant}}" }
      ]
    },
    {
      "type": "bargauge",
      "title": "RPA 账号健康（按平台）",
      "id": 21,
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 12 },
      "targets": [
        { "expr": "sum(admissions_rpa_accounts_logged_in) by (platform)", "legendFormat": "{{platform}}" }
      ]
    },
    {
      "type": "timeseries",
      "title": "AI 调用 · 按场景（最近 24h）",
      "id": 22,
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 12 },
      "targets": [
        { "expr": "admissions_ai_calls_last_day", "legendFormat": "{{scene}}" }
      ]
    },
    {
      "type": "timeseries",
      "title": "RPA 任务 · 按平台状态",
      "id": 30,
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 20 },
      "targets": [
        { "expr": "sum(admissions_rpa_tasks_total) by (platform, status)", "legendFormat": "{{platform}}/{{status}}" }
      ]
    },
    {
      "type": "timeseries",
      "title": "审计日志写入速率（每小时）",
      "id": 31,
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 20 },
      "targets": [
        { "expr": "admissions_audit_writes_last_hour", "refId": "A" }
      ]
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["admissions", "ai"],
  "templating": { "list": [] },
  "time": { "from": "now-24h", "to": "now" },
  "timepicker": {},
  "title": "招生智能体 · 运营总览",
  "uid": "admissions-main",
  "version": 1
}
